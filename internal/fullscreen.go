package kit

// Hometown commands used by fullscreen tools (notes, junk).
// These handle session navigation so the user lands in the right place
// when entering or leaving a fullscreen session.
const (
	fullscreenRecordVisit = "tmux-hometown record-visit"
	fullscreenPrevSession = "tmux-hometown previous-session"
	fullscreenKillSession = "tmux-hometown kill-session -y"
)
